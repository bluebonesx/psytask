{
  "name": "benchmark",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun scripts/build.ts",
    "dev": "bun run build --dev"
  },
  "dependencies": {
    "detect-browser": "^5.3.0"
  }
}
