{
  "name": "playground",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun build-only --watch",
    "build": "bun build-only --production",
    "build-only": "bun scripts/build.ts"
  },
  "dependencies": {
    "@stackblitz/sdk": "^1.11.0"
  },
  "devDependencies": {
    "shared": "workspace:*",
    "@svgdotjs/svg.js": "^3.2.4"
  }
}
